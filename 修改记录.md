# Astra Pro 相机驱动 ROS 2 Jazzy 适配修改记录

## 概述

原驱动包在 Ubuntu 22.04 + ROS 2 Humble 下可正常编译，移植到 ROS 2 Jazzy 后编译失败。
失败原因集中在三类 ROS 2 Jazzy 的接口/头文件改名：

1. `cv_bridge` 头文件由 `.h` 改为 `.hpp`；
2. `image_geometry` 头文件由 `.h` 改为 `.hpp`；
3. `rclcpp` 参数回调类型 `OnParametersSetCallbackType` 从
   `NodeParametersInterface` 移动到 `OnSetParametersCallbackHandle`。

以下修改均依据 `编译日志.txt` 中的报错信息完成。

## 详细修改清单

| 文件 | 位置 | 修改前 | 修改后 | 原因 |
| --- | --- | --- | --- | --- |
| `astra_camera/include/astra_camera/ob_camera_node.h` | 第 19 行 | `#include <cv_bridge/cv_bridge.h>` | `#include <cv_bridge/cv_bridge.hpp>` | Jazzy 中 cv_bridge 头文件已改为 `.hpp`，原 `.h` 已移除，导致 `cv_bridge/cv_bridge.h: No such file or directory` |
| `astra_camera/src/ob_camera_node.cpp` | 第 15 行 | `#include <cv_bridge/cv_bridge.h>` | `#include <cv_bridge/cv_bridge.hpp>` | 同上 |
| `astra_camera/src/uvc_camera_driver.cpp` | 第 15 行 | `#include <cv_bridge/cv_bridge.h>` | `#include <cv_bridge/cv_bridge.hpp>` | 同上 |
| `astra_camera/src/point_cloud_proc/point_cloud_xyzrgb.cpp` | 第 42 行 | `#include <image_geometry/pinhole_camera_model.h>` | `#include <image_geometry/pinhole_camera_model.hpp>` | Jazzy 中 image_geometry 头文件已改为 `.hpp`，原 `.h` 已移除 |
| `astra_camera/src/point_cloud_proc/point_cloud_xyzrgb.cpp` | 第 43 行 | `#include <cv_bridge/cv_bridge.h>` | `#include <cv_bridge/cv_bridge.hpp>` | cv_bridge 头文件改名 |
| `astra_camera/include/astra_camera/point_cloud_proc/point_cloud_xyzrgb.h` | 第 44 行 | `#include <image_geometry/pinhole_camera_model.h>` | `#include <image_geometry/pinhole_camera_model.hpp>` | image_geometry 头文件改名 |
| `astra_camera/include/astra_camera/point_cloud_proc/point_cloud_xyzrgb.h` | 第 46 行 | `#include <cv_bridge/cv_bridge.h>` | `#include <cv_bridge/cv_bridge.hpp>` | cv_bridge 头文件改名 |
| `astra_camera/src/point_cloud_proc/point_cloud_xyz.cpp` | 第 37 行 | `#include <image_geometry/pinhole_camera_model.h>` | `#include <image_geometry/pinhole_camera_model.hpp>` | image_geometry 头文件改名 |
| `astra_camera/include/astra_camera/point_cloud_proc/point_cloud_xyz.h` | 第 37 行 | `#include <image_geometry/pinhole_camera_model.h>` | `#include <image_geometry/pinhole_camera_model.hpp>` | image_geometry 头文件改名 |
| `astra_camera/include/astra_camera/ros_param_backend.h` | 第 22 行 | `rclcpp::node_interfaces::NodeParametersInterface::OnParametersSetCallbackType` | `rclcpp::node_interfaces::OnSetParametersCallbackHandle::OnSetParametersCallbackType` | Jazzy 中该回调类型已不再定义于 `NodeParametersInterface`，改由 `OnSetParametersCallbackHandle` 提供，原写法导致 `has not been declared` 编译错误 |
| `astra_camera/src/ros_param_backend.cpp` | 第 27 行 | `rclcpp::node_interfaces::NodeParametersInterface::OnParametersSetCallbackType` | `rclcpp::node_interfaces::OnSetParametersCallbackHandle::OnSetParametersCallbackType` | 同上 |
| `astra_camera/include/astra_camera/constants.h` | 第 17 行新增 | （无 `<cstdint>`） | `#include <cstdint>` | 文件使用 `uint32_t` 但未包含 `<cstdint>`，旧编译器依赖传递包含可编译，Jazzy 的 GCC 13 不再传递该头文件，导致 `uint32_t does not name a type` |
| `astra_camera/include/astra_camera/point_cloud_proc/depth_traits.h` | 第 38 行新增 | （无 `<cstdint>`） | `#include <cstdint>` | 文件使用 `uint16_t`、`uint8_t`，此前依赖其它头文件传递包含 `<cstdint>`，补全后使该头文件可独立包含 |
| `astra_camera/include/astra_camera/ob_timer_filter.h` | 第 15 行新增 | （无 `<cstddef>`） | `#include <cstddef>` | 文件使用 `std::size_t`，补全其所在的 `<cstddef>`，避免依赖传递包含 |
| `astra_camera/include/astra_camera/ob_camera_node_factory.h` | 第 16 行新增 | （无 `<cstddef>`） | `#include <cstddef>` | 文件使用 `size_t`，补全 `<cstddef>`，避免依赖传递包含 |
| `astra_camera/include/astra_camera/uvc_camera_driver.h` | 第 17 行新增 | （无 `<cstddef>`） | `#include <cstddef>` | 文件使用 `size_t`，补全 `<cstddef>`，避免依赖传递包含 |

## 说明

- `cv_bridge` 与 `image_geometry` 仅头文件扩展名发生变化，内部类/函数接口
  （如 `cv_bridge::CvImage`、`cv_bridge::toCvCopy`、`image_geometry::PinholeCameraModel` 等）
  在 Jazzy 中保持不变，无需改动调用代码。
- `add_on_set_parameters_callback` 在 Jazzy 中仍返回
  `rclcpp::node_interfaces::OnSetParametersCallbackHandle::SharedPtr`，
  因此 `ros_param_backend.cpp` 析构函数中对
  `rclcpp::node_interfaces::OnSetParametersCallbackHandle` 的转换无需修改。
- Jazzy 中该回调类型的正名为 `OnSetParametersCallbackHandle::OnSetParametersCallbackType`，
  旧的 `OnParametersSetCallbackType` 仅作为兼容别名存在，因此源码统一使用正名。
- `constants.h` 中使用的 `uint32_t` 依赖 `<cstdint>` 显式声明，属于编译环境
  （GCC 版本差异导致的传递包含变化）带来的问题，与 ROS 版本接口改名无关，一并补全。
- 对项目自有头文件做了全面排查，将使用固定宽度整数类型（`uint16_t`/`uint8_t`）的
  `depth_traits.h` 补全 `<cstdint>`，将使用 `size_t`/`std::size_t` 的
  `ob_timer_filter.h`、`ob_camera_node_factory.h`、`uvc_camera_driver.h` 补全 `<cstddef>`，
  使这些头文件不再依赖其它头文件的传递包含，避免在 GCC 13 下出现同类编译错误。
- 本次仅修复编译日志中出现的错误；`astra_camera_msgs` 包已能正常编译，未作改动。
